{
  "buildCommand": "npm install && npx --yes vite --config apps/web/vite.config.ts build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "echo 'handled by buildCommand'",
  "framework": "vite"
}
