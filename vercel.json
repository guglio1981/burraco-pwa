{
  "buildCommand": "NODE_ENV=development npm install && node_modules/.bin/vite --config apps/web/vite.config.ts build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "echo 'handled by buildCommand'",
  "framework": "vite"
}
